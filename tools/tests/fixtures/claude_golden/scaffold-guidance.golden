  - Claude Code lifecycle hardened out of the box: .claude/settings.json fires session-start + post-write validation automatically; /end-session + /retrospective shortcuts are installed. VS Code Copilot lifecycle remains separately unverified. Delete .claude/ to fall back to interpretation-only — the domain kernel still drives both.
