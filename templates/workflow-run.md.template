---
id: run-[instance-name]
type: workflow-run
status: active           # reserved: active | paused | complete | abandoned
created: [ISO-date]
current_stage: [stage-id]          # MUST be a stage id in the linked definition
held_by: [operator-or-agent-id]    # advisory claim; omit when the run is unheld
linked_things:
  - id: [process-name]-definition
    relation: instance-of          # the definition pointer (do NOT add a `definition:` field)
  # Accrued decisions link here as they are made (provenance.md):
  # - id: decision-[some-call]
  #   relation: informs
---

# Run: [Human-readable title for this instance]

## Where This Is

[One paragraph: the current stage, why the run is there, and what the last
transition was. This is the resume point — write it so the next session reads
its state rather than reconstructing it from scratch.]

## Next

[The immediate next move, and what it is blocked on if anything.]

## Notes

[Anything instance-specific worth carrying forward. Do NOT keep a stage_history
list here — git's commit log is the history of this run's cursor changes.]
